# Upload Fixtures

Place local MRI upload fixtures here for E2E tests, or set `NEUROCADE_UPLOAD_FIXTURES_DIR` to another directory.

This directory is separate from `HOST_DATA_DIR`; uploaded files created by the app are stored in the case directory under `HOST_DATA_DIR/output`.
